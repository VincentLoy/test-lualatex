\documentclass{article}
\usepackage{luacode}
 
\begin{document}

% une liste !
  Un liste de nombres aléatoires :
  \begin{itemize}
    \item \directlua{ tex.print(math.random()) }
    \item \directlua{ tex.print(math.random()) }
    \item \directlua{ tex.print(math.random()) }
    \item \directlua{ tex.print(math.random()) }
    \item \directlua{ tex.print(math.random()) }
  \end{itemize}

\end{document}